import { Assets } from "pixi.js";

export async function loadAssets() {
    const manifest = {
        bundles: [{ 
            name: 'main',
            assets: [
                {}
            ]
        }]
    }
}